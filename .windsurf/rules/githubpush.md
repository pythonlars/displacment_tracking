---
trigger: model_decision
description: when ever i say something like "push to github"
---

Whenever I say "push to github", you must push to https://github.com/pythonlars/displacment_tracking as a new branch called displacment_tracking (using semantic versioning: if the last push was main, this one should be displacment_tracking_v1.1.1), with a commit message describing the changes made since the last push. If you do not understand, say "clarify" and nothing else.

